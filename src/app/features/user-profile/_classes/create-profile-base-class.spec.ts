import { CreateProfileBaseClass } from './create-profile-base-class';

describe('CreateProfileBaseClass', () => {
  it('should create an instance', () => {
    expect(new CreateProfileBaseClass()).toBeTruthy();
  });
});
